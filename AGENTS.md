# Custom Project Instructions

* **GitHub Sync Workflow**: When the user requests an update from GitHub, use `degit` to pull the specific repository (`Mahir-Ahmed-250/Trendify`), overlay the files onto the current workspace, install dependencies, and restart the dev server.
* **Database Operations Constraint**: **NEVER** remove or delete any data from the database. All existing data must be strictly preserved.
* **Strict Additive-Only Development (LIVE APP)**: The application is currently LIVE in production. When adding new features, you MUST NOT change, rewrite, or break any existing logic or functions. New features must be added separately. Do not modify the existing core workflows.